<div>	
	<p>
	  <strong>Nombre de la Compañia</strong>
	  <%= @company.name %>
	</p>

	<%= form_for([@company, @claim], remote: true) do |f| %>
		<%= f.label :content, 'Agregar un Reclamo' %>
		<%= f.text_area :content %>
		<%= f.submit 'Enviar' %>
	<% end %>

</div>

<div>
	
	<p>Company Claims</p>
	<ul id="claims">
	    <%= render @claims %>
	</ul>

</div>

<%= link_to 'Edit', edit_company_path(@company) %> |
<%= link_to 'Back', companies_path %>
